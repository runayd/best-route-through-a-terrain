import { BackgroundGradientPipe } from './background-gradient.pipe';

describe('BackgroundGradientPipe', () => {
  it('create an instance', () => {
    const pipe = new BackgroundGradientPipe();
    expect(pipe).toBeTruthy();
  });
});
